@model IEnumerable<MusicPlaylistOrganizer.Models.Artist>

<h1>Artists</h1>

<ul>
    @foreach (var artist in Model)
    {
        <li>
            <a href="/Artists/Details/@artist.ArtistID">@artist.Name</a>
            (Tracks: @artist.Tracks?.Count ?? 0)
        </li>
    }
</ul>
